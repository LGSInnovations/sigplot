language: node_js
node_js:
  - "node"

services:
  - docker

before_install:
  - rm -rf dist/*
  - npm install -g grunt
  - npm install -q
script:
  - grunt jsbeautifier:cleanup
  - grunt dist

notifications:
  email:
    on_success: never

